// Phase-23.1 wip: RBAC tests are being rewritten for the scoped model.
// See plan: scoped grants (org vs global), explicit org_members,
// path-based org context. The old flat assign_role / user_roles tests no
// longer compile and are deferred until the new role-assignment API ships.

#[test]
fn rbac_tests_pending_phase_23_1_rewrite() {}
