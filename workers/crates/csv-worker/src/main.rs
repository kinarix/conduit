// csv-worker scaffolding. Topic: csv.read / csv.write.
//
// Implementation is intentionally deferred to a follow-up commit. The
// crate exists so the workspace shape is set and `cargo build` covers
// the worker fleet end-to-end.

fn main() {
    eprintln!(
        "csv-worker is scaffolded but not yet implemented. \
         Track progress in the conduit-workers repo."
    );
    std::process::exit(2);
}
