// kafka-produce-worker scaffolding. Topic: kafka.produce.
//
// Implementation is intentionally deferred to a follow-up commit. The
// crate exists so the workspace shape is set.

fn main() {
    eprintln!(
        "kafka-produce-worker is scaffolded but not yet implemented. \
         Track progress in the conduit-workers repo."
    );
    std::process::exit(2);
}
